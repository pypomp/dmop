% Captions for the PNG benchmark figures.

\caption{Raincloud plots of the Dacca searches. \textbf{A}, comparable
computational effort, with 100 Ditlevsen fits limited to 800 seconds;
74 of 100 Ditlevsen fits are visible above the
$-4300$ display cutoff, and their full-sample median is
$-4100.40$. Of these fits, 90 reached the time budget and
10 ended early after a numerical failure. \textbf{B}, the
manuscript's extended-effort reference runs with its original
$-3800$ cutoff; Ditlevsen was not run at the
extended budget. Ditlevsen fitting used $J=5000$; each retained
checkpoint was evaluated under the Euler--20 model with $J=5000$
and 36 replicates. Parameters were taken at the first completed score evaluation crossing 700 seconds; fits that failed earlier use their last available iterate.}

\caption{Selected estimates for nine Dacca parameters. The original methods
use the manuscript's extended-effort runs. The Ditlevsen density contains
100 bounding-box starts limited to 800 seconds, using the retained
checkpoint from each fit. Parameters were taken at the first completed score evaluation crossing 700 seconds; fits that failed earlier use their last available iterate.}

\caption{Optimization progress against elapsed time. The manuscript's
extended-effort trajectories and the Ditlevsen fits are displayed through
800 seconds. Ditlevsen traces were evaluated under the Euler--20 model
with $J=5000$ and one replicate. Heavy lines show medians;
shading extends from the 10th percentile to the maximum. Failed searches are
carried forward from their last available checkpoints. The lower display limit
is -4300.}


% Captions for the PNG validation figures.  Keep prose out of the graphics.

\caption{Raincloud plot of the Dacca searches. \textbf{A}, comparable
computational effort: the Ditlevsen row contains the three 20-update validation
runs. \textbf{B}, extended computational effort: the Ditlevsen row contains the
single 80-update validation run. The four manuscript methods contain 100 runs
per panel. The horizontal range is extended below the manuscript's plotting
cutoff so that the Ditlevsen runs remain visible. The dotted red line marks the
best retained likelihood in each panel.}

\caption{Final estimates for nine Dacca parameters under extended computational
effort. The colored densities reproduce the 100 manuscript runs for each
method. The black Ditlevsen line is the single 80-update validation estimate;
the final experiment will replace it with a density over 100 starts.}

\caption{Optimization progress against elapsed time under extended
computational effort. Colored lines and bands show the median and the interval
from the 10th percentile to the maximum over 100 manuscript runs. The black
line is the 80-update Ditlevsen validation run; its band is two Monte Carlo
standard errors on either side of the independently evaluated Euler-20
log-likelihood. The plotted range follows the manuscript's $-3800$ cutoff; the
Ditlevsen validation trajectory remains below it.}

\caption{Ditlevsen filter diagnostics as the number of process substeps per
month increases. \textbf{A}, largest invalid-particle fraction. \textbf{B},
smallest effective sample size among runs that did not collapse.}
